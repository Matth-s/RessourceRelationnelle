services:
  mon-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__Default=Data Source=/app/data/data.db3
    volumes:
      - sqlite_data:/app/data

volumes:
  sqlite_data: